"""This module contains necessary database parameters required by sendfeeds module to connect to database for fetching data.
"""

import MySQLdb as mdb

def dbconnect():
    """return MySQL cursor.
    Function to establish connection to MySQL database.
    """
    conn = None

    try:
        conn = mdb.connect (host = " ",
                            user = " ",
                            passwd = " ",
                            db = "Temp",
                            charset='utf8',
                            use_unicode = True)
        conn.autocommit(True)      # This is needed for storage engine InnoDB (for foreign keys). It won't save values in tables until commited
        cursor = conn.cursor()
        return cursor

    except mdb.Error, e:
        print e
        sys.exit(1)
