
"""
This file contains global variables used in various customised files.
Any change needed shall be made here and it would get reflected in customised files

"""
# Sensor name. Used in queue.py module while naming file before saving it in queue
# Naming convention - don't use '-' in sensor name, else it will break the following regex:
# mailFields['sourceIP'] = key.split("-")[-2]
# mailFields['sensorID'] = key.split("-")[-1]
sensorName = 'SensorMH'
